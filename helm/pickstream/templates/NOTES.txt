Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ .Values.namespace }}
  $ helm get all {{ .Release.Name }} -n {{ .Values.namespace }}

📦 Deployed Components:
  ✓ Backend Service: {{ .Values.backend.name }}
  ✓ Frontend Service: {{ .Values.frontend.name }}
{{- if .Values.ingress.enabled }}
  ✓ Ingress Controller
{{- end }}
{{- if .Values.backend.autoscaling.enabled }}
  ✓ Backend HPA ({{ .Values.backend.autoscaling.minReplicas }}-{{ .Values.backend.autoscaling.maxReplicas }} replicas)
{{- end }}
{{- if .Values.frontend.autoscaling.enabled }}
  ✓ Frontend HPA ({{ .Values.frontend.autoscaling.minReplicas }}-{{ .Values.frontend.autoscaling.maxReplicas }} replicas)
{{- end }}

🔍 Check the status of your deployment:

  $ kubectl get pods -n {{ .Values.namespace }}
  $ kubectl get svc -n {{ .Values.namespace }}
  $ kubectl get ingress -n {{ .Values.namespace }}

{{- if .Values.ingress.enabled }}

🌐 Access your application:

{{- range .Values.ingress.hosts }}
  URL: https://{{ .host }}
{{- end }}

{{- else }}

🌐 Access your application via port-forward:

  Backend:
  $ kubectl port-forward -n {{ .Values.namespace }} svc/{{ .Values.backend.name }} 8080:8080
  URL: http://localhost:8080

  Frontend:
  $ kubectl port-forward -n {{ .Values.namespace }} svc/{{ .Values.frontend.name }} 8080:80
  URL: http://localhost:8080

{{- end }}

📊 View logs:

  Backend:
  $ kubectl logs -f -n {{ .Values.namespace }} -l app.kubernetes.io/component=backend

  Frontend:
  $ kubectl logs -f -n {{ .Values.namespace }} -l app.kubernetes.io/component=frontend

{{- if .Values.monitoring.enabled }}

📈 Prometheus Metrics:

  Backend metrics available at:
  $ kubectl port-forward -n {{ .Values.namespace }} svc/{{ .Values.backend.name }} 8080:8080
  URL: http://localhost:8080/actuator/prometheus

{{- end }}

🔧 Troubleshooting:

  If pods are not starting, check events:
  $ kubectl get events -n {{ .Values.namespace }} --sort-by='.lastTimestamp'

  Describe problematic pods:
  $ kubectl describe pod <pod-name> -n {{ .Values.namespace }}

  Check resource usage:
  $ kubectl top pods -n {{ .Values.namespace }}

For more information, visit:
  - GitHub: {{ .Chart.Home }}
  - Documentation: https://github.com/yourusername/pickstream-app/tree/main/docs
